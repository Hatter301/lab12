#include <iostream>
using namespace std;
int Sign(double x) {
	if (x < 0)
		x = -1;
	else if (x == 0)
		x = 0;
	else if (x > 0)
		x = 1;
	return x;
}
double a, b;
int main() {
	cin >> a >> b;
	cout << Sign(a)+Sign(b);
}
